rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write access to all documents during the retreat period
      // For production, these should be restricted based on Auth roles
      allow read, write: if request.time < timestamp.date(2026, 8, 31);
    }
  }
}
